#include<stdio.h>

int main()
{
    int a,b,sum;
    scanf("%d %d",&a,&b);
    sum=a+b;
    if (sum>15 && sum<20)
    {
       printf("20");
    }
    else
    {
       printf("sum");
    }
   return 0;
}   
