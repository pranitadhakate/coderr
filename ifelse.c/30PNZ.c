#include<stdio.h>

int main()
{
    int N;
    scanf("%d",&N);
    if (N==0)
    {
       printf("N is zerro");
    }
    else if (N>0)
    {
       printf("N is positive");
    }
    else
    {
       printf(" N is negative");
    }
   return 0;
}   
